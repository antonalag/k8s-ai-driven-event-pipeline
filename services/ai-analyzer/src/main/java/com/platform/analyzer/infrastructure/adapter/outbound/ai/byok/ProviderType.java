package com.platform.analyzer.infrastructure.adapter.outbound.ai.byok;

/**
 * Supported BYOK provider types determining payload format and response extraction strategy.
 */
public enum ProviderType {
    OPENAI_COMPATIBLE,
    CUSTOM
}
